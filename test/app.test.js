const { describe, it } = require('node:test');
const assert = require('node:assert');

// This test file demonstrates the use of Node.js v22's built-in test runner
describe('Partner Samples App', () => {
  it('should run a basic test', () => {
    assert.strictEqual(1 + 1, 2);
  });

  // Add more tests for your application here
}); 