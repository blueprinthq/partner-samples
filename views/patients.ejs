<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css" />
    <title>My EHR - Patient List</title>
</head>
<body>
    <h1>My EHR Patient List</h1>
    <ul>
        <% items.forEach(item => { %>
            <li>
                <div>
                    <a href="/patients/<%= item.id %>"><%= item.name %></a> - <%= item.age %> years old (embedded UI using backend APIs)
                    <ul>
                        <li><a href="/patients/<%= item.id %>?chartStyle=uiOnly">UI Only Version</a> (not using backend APIs)</li>
                        <li><a href="/patients/<%= item.id %>?chartStyle=miniWidget">Minified embedded UI with custom styles</a></li>
                        <li><a href="/patients/<%= item.id %>?chartStyle=iframe">IFrame Version</a> (deprecated)</li>
                    </ul>
                </div>
            </li>
        <% }) %>
    </ul>
</body>
</html>
